程序介绍:
    完成FTP的功能
    1. 用户加密认证 (完成)
    2. 允许同时多用户登录 (完成)
    3. 每个用户有自己的家目录 ，且只能访问自己的家目录(完成)
    4. 对用户进行磁盘配额，每个用户的可用空间不同(完成,留出一个充值的API,服务器通过此API来改变用户信息，从而改变磁盘配额)
    5. 允许用户在ftp server上随意切换目录cd(完成)
    6. 允许用户查看当前目录下文件ls,dir(完成)
    7. 允许上传put和下载get文件(完成)，保证文件一致性(完成)
    8. 文件传输过程中显示进度条(完成)
    9. 断点续传(未完成)

    登录注册一级视图：
    1. 注册
    2. 登录
    q. 退出

        功能显示二级视图(输入相关命令运行相应功能)
        1. 上传文件到服务器(put)
        2. 从服务器端下载文件(get)
        3. 改变文件路径(cd)
        4. 查看文件目录(ls,dir)
        5. 查看当前路径(pwd)
        6. 冲会员
        q. 退出服务





程序结构:
FTPClient/
├── README
├──  #主程序目录
│   ├── __init__.py
│   ├── bin
│   │   ├── __init__.py
│   │   ├── start.py  #执行程序
│   ├── conf #配置文件
│   │   ├── __init__.py
│   │   └── settings.py  #用于配置服务器地址和接收路径等常亮
│   │   └── common.py    #定义了一个自己的sock类，类里面定义了connect函数以及包头的接收receive_unpack函数和报头发送send_pack函数
│   ├── core #主要程序逻辑都 在这个目录里
│   │   ├── __init__.py
│   │   ├── ftp_client.py  #定义了一个MyFTPClient类，里面定义的注册登录时信息字典的传输函数
│   │   ├── main.py         #程序的主逻辑:分为注册登录一级菜单和客户端上传下载等功能的二级菜单，定义了一个start类继承了ftp_client和my_sock类
│   ├── db
│   ├── file_download  #用户下载目录

