程序介绍:
    完成FTP的功能
    1. 用户加密认证 (完成)
    2. 允许同时多用户登录 (完成)
    3. 每个用户有自己的家目录 ，且只能访问自己的家目录(完成)
    4. 对用户进行磁盘配额，每个用户的可用空间不同(完成,留出一个充值的API,服务器通过此API来改变用户信息，从而改变磁盘配额)
    5. 允许用户在ftp server上随意切换目录cd(完成)
    6. 允许用户查看当前目录下文件ls,dir(完成)
    7. 允许上传put和下载get文件(完成)，保证文件一致性(完成)
    8. 断点续传(未完成)



程序结构:
FTPServer/
├── README
├──  #主程序目录
│   ├── __init__.py
│   ├── bin
│   │   ├── __init__.py
│   │   ├── start.py  #执行程序
│   ├── conf #配置文件
│   │   ├── __init__.py
│   │   └── settings.py  #用于配置各种信息
│   │   └── common.py    #定义了一个自己获取目录大小的函数
│   ├── core #主要程序逻辑都 在这个目录里
│   │   ├── __init__.py
│   │   ├── main.py      #使用socketserver支持并发，且定义了注册，登录，上传，下载等函数
│   ├── db
│   │   ├── user        # 存放用户信息
│   │       └── tsy     # 用户tsy信息
│   │       └── lyt
│   ├── home  #用户家目录
│   │   ├── tsy      # 用户tsy家目录
│   │       └── study
│   │   └── lyt      # 用户lyt家目录