程序要求
'''
简单的主机管理工具
1. 主机分组
2. 登录后显示主机分组，选择分组后查看主机列表
3. 可批量执行命令，发送文件
4. 主机用户名密码可以不同  # 用于ssh连接，发送命令以及文件
'''

程序注意事项：
1.  主机信息全都放在host.info.conf文件里，采用的格式为conf标准格式。但是由于组员的信息不仅仅一条，所以采用
    了字典的形式存储组员的信息，且存储的格式为json标准格式，方便在程序中转换为字典
2.  读取文件信息采用了configparser模块。在选组后，需要显示组员信息，且在后面远程发送文件和执行命令也需要用到
    组员信息，所以在common文件中定义一个读取组员信息的列表的函数read_group_info_dic，
    返回一个组的组员信息列表。
3.  采用了paramiko模块来对远程主机进行命令操作和文件执行。ssh的连接用到了上诉2中的列表。


程序结构:
FTPServer/
├── README
├──  #主程序目录
│   ├── __init__.py
│   ├── bin
│   │   ├── __init__.py
│   │   ├── start.py  #执行程序
│   ├── conf #配置文件
│   │   ├── __init__.py
│   │   ├── settings.py     #用于配置各种信息，存放了登录名和登录密码
│   │   ├── common.py       #定义了一个组员信息列表函数
│   │   └── host_conf.conf  #存放了所有的主机信息
│   ├── core #主要程序逻辑都 在这个目录里
│   │   ├── __init__.py
│   │   ├── main.py      #程序运行入口
