python支持多进程和多线程。但是由于GIL，同一时刻，python只能运行单线程(即使有多个CPU,但是可以多进程并行执行)


进程是最小的资源管理单位，一个进程里面可以有多个线程。一个进程里面有程序、数据集、进程控制块。进程中的线程共享这些资源
进程
1. 单个CPU


2. 多个CPU

线程锁：
一个进程中可以有多个线程，进程是最小的资源单位。也就是进程里的所有线程都共享进程中的数据。所以在线程的切换对的时候
有可能会线程1和线程2同时对数据进行修改，这样数据就不安全。给线程加上锁。线程锁同一个时间只有一个线程拥有，其他
线程没有这个锁，保证了锁定数据的安全性。只有当锁定的数据执行完毕后，其他线程才能得到该锁。