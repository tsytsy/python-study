0.1版本
1. 只实现了两个人之间的通信，不能开房间群聊
2. 只能一次发送，不能循环发送
3. 服务器虽然可以和两个人聊天，但是要回复完一个人之后才能接受另外一个人的消息
    只有回复了一条消息才能收到下一条消息
4. A发送完消息给服务器，服务器收到后，处于回复状态。如果A想再发信息，虽然打字了，按下enter键，但是必须得等到收到消息
    后才会再次发送。
    A发完给服务器后，服务器处在回复状态，但是此时如果B给服务器发消息，虽然服务器没有读到这个消息，到时
    B的消息是可以到达服务器的，只是在服务器的内存里，因为B知道IP和端口，网服务器发，服务器底层就可以收的到。
5. 服务器不开启，会报错？？